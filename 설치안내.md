# 홈페이지 인터넷에 올리기 + 문자 자동발송 설치 안내

전부 무료입니다. 도메인만 연 1~2만원입니다.

---

## 1단계 — 솔라피 준비 (지금 바로 시작하세요, 승인에 1~2일 걸립니다)

1. **solapi.com** 가입
2. **발신번호 등록** — `010-4483-0550`
   - 통신서비스 이용증명원(통신사 앱에서 발급) 또는 사업자등록증 제출
   - 승인까지 보통 1~2일 걸리므로 **이것부터** 해두세요
3. **API 키 발급** — 개발/연동 → API Key 관리 → 새 키 발급
   - **API Key** 와 **API Secret** 두 개를 복사해 두세요 (Secret은 발급 때 한 번만 보입니다)
4. **충전** — 5,000원만 넣어도 SMS 약 250건입니다

---

## 2단계 — Netlify에 홈페이지 올리기 (10분)

1. **netlify.com** 접속 → **Sign up** (구글 계정으로 가입 가능)
2. 로그인 후 **Add new site → Deploy manually**
3. 이 폴더 전체를 화면에 **끌어다 놓기**
   ```
   index.html            ← 홈페이지 (가선영속눈썹연구소.html 이름 바꾼 것)
   관리자.html
   robots.txt
   sitemap.xml
   netlify.toml
   netlify/functions/notify.js
   ```
4. 잠시 뒤 `random-name-123.netlify.app` 같은 주소가 생깁니다 → **이미 인터넷에 공개된 상태입니다**

---

## 3단계 — 키 숨겨서 넣기 (제일 중요)

Netlify 사이트 화면에서 **Site configuration → Environment variables → Add a variable**

아래를 하나씩 추가합니다.

| 이름 | 값 |
|---|---|
| `TELEGRAM_TOKEN` | `8849981361:AAHwtodNBMt5KMA7537MjLM_uY3l0XPHLak` |
| `TELEGRAM_CHAT` | `8876305411` |
| `SOLAPI_KEY` | 솔라피 API Key |
| `SOLAPI_SECRET` | 솔라피 API Secret |
| `SMS_FROM` | `01044830550` |

넣은 뒤 **Deploys → Trigger deploy → Deploy site** 를 한 번 눌러주세요.

> 이 값들은 **서버 안에만** 저장됩니다. 손님이 홈페이지 소스를 봐도 절대 안 보입니다.

---

## 4단계 — 도메인 연결 (선택, 하지만 권장)

1. **가비아** 또는 **후이즈**에서 도메인 구입 (예: `kasunyoung.com`)
2. Netlify → **Domain management → Add a domain** → 구입한 주소 입력
3. 화면에 나오는 **네임서버 4개**를 도메인 산 곳의 관리 화면에 입력
4. 몇 시간 뒤 연결됩니다. HTTPS 자물쇠는 자동으로 붙습니다

---

## 5단계 — 검색 등록

| 사이트 | 할 일 |
|---|---|
| **네이버 서치어드바이저** (searchadvisor.naver.com) | 사이트 등록 → 소유확인 → sitemap.xml 제출 |
| **구글 서치콘솔** (search.google.com/search-console) | 동일 |

`sitemap.xml` 파일 안의 `https://내도메인.com/` 을 실제 주소로 바꾸는 것을 잊지 마세요.

---

## 작동 방식

손님이 **[문의 남기기]** 를 누르면

1. 홈페이지가 서버 함수(`notify`)를 호출합니다
2. 서버가 **원장님 텔레그램**으로 문의 내용을 보냅니다
3. 서버가 **손님 휴대폰**으로 안내 문자를 보냅니다

```
[가선영속눈썹연구소]
문의가 정상 접수되었습니다.
확인 후 순차적으로 연락드리겠습니다.
문의 010-4483-0550
```

4. 손님 화면에 "문의가 정상 접수되었습니다" 안내가 뜹니다

서버가 잠깐 안 되더라도 **텔레그램·이메일로 자동 우회**하므로 문의가 사라지지 않습니다.

---

## 안내문자 내용을 바꾸고 싶을 때

`netlify/functions/notify.js` 파일에서 `guestText` 부분을 고치면 됩니다.

```js
const guestText =
  `[${SHOP_NAME}]\n문의가 정상 접수되었습니다.\n확인 후 순차적으로 연락드리겠습니다.\n문의 ${SHOP_TEL}`;
```

**한글 45자(90byte)를 넘기면** 문자 요금이 SMS(약 20원)에서 LMS(약 40원)로 올라갑니다. 지금 문구는 SMS 범위 안에 있습니다.

---

## 도움이 필요하시면

각 단계 화면을 사진으로 보내주시면 바로 알려드리겠습니다.
특히 **3단계 환경변수**는 오타가 나기 쉬우니 넣으신 뒤 화면을 한 번 보여주세요.
